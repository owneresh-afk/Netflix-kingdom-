services:
  - type: web
    name: netflix-kingdom-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: 8080
